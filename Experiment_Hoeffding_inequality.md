# Hoeffding's inequality
The main idea behind the following experiment is to show that Hoeffding's inequality does not hold in certain cases.
However, before describing the experiment, we must recall Hoeffding's inequality
```math
  \mathbb{P}(|\nu-\mu| ≥ \epsilon) ≤ 2Me^{-2N\epsilon^{2}}
```
where
